OWNER_USERNAME=owner
# Run node scripts/setup-demo.mjs locally to generate a hash and password.
OWNER_PASSWORD_HASH=
APP_ORIGIN=http://127.0.0.1:3000
DEMO_MODE=true
# Required in production: mount a persistent disk here, e.g. /var/data/wgc
# DATA_DIR=/var/data/wgc
